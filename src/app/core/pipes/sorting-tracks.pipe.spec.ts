import { SortingTracksPipe } from './sorting-tracks.pipe';

describe('SortingTracksPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingTracksPipe();
    expect(pipe).toBeTruthy();
  });
});
